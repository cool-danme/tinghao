package tinghao04;

/*  装箱：把基本数据类型转换为对应的包装类型
    拆箱：把包装类型转换为对应的基本数据类型
    注意：在使用包装类型的时候，如果做操作，最好先判断是否为null,只要是对象就必须进行不为null的判断
* */

public class IntegerDemo {
    public static void main(String[] args) {
        //装箱：把基本数据类型转换为对应的包装类型
        Integer i=Integer.valueOf(100);
        Integer ii=100;//自动装箱
        //拆箱：把包装类型转换为对应的基本数据类型
        ii+=200;//自动拆箱
        //ii=ii.intValue()+200;
        System.out.println(ii);
        Integer iii=null;
        if (iii!=null){//NullPointerException
        iii+=300;
        }
    }
}
